.card
  .content
    .header= tournament.name
    .meta Tennis
    .description Players: #{tournament.players.count}
  = link_to 'View Tournament', tournament_path(tournament), class: 'ui bottom attached button'
